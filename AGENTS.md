# 本仓库 Git 工作流

以下规则仅适用于本仓库的维护任务，不属于安装到其他项目的模型分工规则。
用户已持续授权按此流程拉取、提交和推送，无需每次重新询问。

## 每日首次任务

- 在执行任务内容前，检查当前分支、上游分支和工作区状态。
- 使用设备本地日期（`yyyy-MM-dd`），读取
  `git config --local --get modelRouting.lastPullDate`；配置不存在视为尚未拉取。
- 如果记录不是今天，执行 `git pull --ff-only`。成功后才将今天写入
  `git config --local modelRouting.lastPullDate <日期>`；同一天后续任务可跳过拉取。
- 若有未提交改动，先检查其归属；不要为拉取自动丢弃、覆盖或暂存用户改动。
  若无法安全拉取，或遇到网络错误、分支分歧、缺少上游，报告原因，
  不记录成功，也不声称已同步；仅继续不依赖同步的工作。

## 每次任务完成

- 先检查差异并执行与本次改动相适应的验证。
- 仅暂存本任务产生的改动；保留用户或其他任务已有的修改。
  有混合修改时按块暂存，归属无法判断的内容不要自动纳入提交。
- 有本任务改动时，以清晰描述本次变化的消息提交，然后执行 `git push`
  推送到当前分支配置的上游。没有本任务改动时不创建空提交。
- 不强制推送、不改写历史，也不擅自推送已有且归属不明的本地提交。
  推送被拒绝、上游缺失或检查失败时保留现场并报告具体原因。
- 最终回复说明提交和推送结果；失败时明确指出尚未完成的步骤。
