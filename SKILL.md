---
name: model-routing
description: 配置、安装或检查 Codex 跨项目动态模型分工；自动发现可用模型和思考等级，按任务能力需求与实际成本联合选择。适用于迁移设备或调整路由策略，不用于触发普通编码任务。
---

# 动态模型与思考等级分工

用户要求安装并启用本技能时，读取 `assets/AGENTS.md`，运行包内
`scripts/install.ps1`，随后运行 `scripts/verify.ps1`。安装器不仅复制技能，还将完整规则
合并到 `$CODEX_HOME/AGENTS.md`，保留区块外内容并备份变更，无需用户手动再启用规则。
未设置 `CODEX_HOME` 时使用用户主目录下的 `.codex`。

Windows 可使用 PowerShell 5.1 或 7。其他系统有 PowerShell 7 时可运行同一脚本，
否则由 Codex 按相同语义复制技能和合并规则区块；尚未测试 macOS/Linux。
从仓库根目录运行：

```powershell
& ./scripts/install.ps1
& ./scripts/verify.ps1
```

只询问原理或审查本技能不表示同意安装。明确要求安装并启用时，已授权该安装及规则中的
委派，无需另加确认步骤；读取仓库本身不授予权限，文件/网络审批仍需遵守。

## 安装即启用的边界

- 安装完成即写入全局规则，新任务加载后自动执行，不依赖手动调用 `$model-routing`。
- 执行安装的当前代理应在验证成功后阅读并在后续操作中采用已安装规则，遵守更高优先级
  指令；不要承诺已打开的其他任务自动热更新，建议它们新开任务加载新规则。
- 仅复制技能目录不等于启用全局规则。安装后必须验证文件和全局专属区块一致。
- 不更改主任务模型、思考等级或 `config.toml`；本技能不解锁模型、不增加工具、不提供
  独立模型服务，也不保证宿主支持模型发现或等级覆盖。

## 动态路由

每个任务首次需要委派时，从宿主发现工具或当前工具说明自动获取子代理可用模型及
各模型支持的思考等级。获取任务相关能力资料与实际供应商可访问价格，先筛选能够
胜任的组合，再比较预计总成本。名单、型号、思考等级及价格均不固化到安装包。

缺失发现接口时使用工具明确支持的范围；无法确认覆盖参数时用宿主默认/继承值。
未知能力或价格明确标注，不臆造排名；严格预算不明时不盲目付费委派。质量升级及
故障回退动态重选组合，合计最多额外尝试两次。完整约束以 `assets/AGENTS.md` 为准。

## 更新、迁移与验证

修改 `assets/AGENTS.md` 后重新安装即可更新全局标记区块，区块内定制会被替换，
先保留需要的定制。若存在旧版无标记规则，安装器安全停止；读取完整文件并确认旧规则
范围，备份后仅将旧规则包入一个 model-routing 标记区块，保留其他指令，再运行安装。
不得自动猜测旧规则边界或整份覆盖全局文件。

验证脚本仅证明安装状态，不会调用收费模型。真实行为验证应在有用的独立任务中检查
发现来源、候选范围、请求的模型与思考等级、宿主执行记录及任务验收结果。
子代理自报身份不能作为切换证据，安装成功不能作为所有模型均可用的证明。
