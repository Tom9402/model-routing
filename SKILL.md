---
name: model-routing
description: 配置、安装或检查 Codex 跨项目模型分工规则；按任务复杂度和实际可用模型委派子代理。适用于迁移设备或调整路由策略，不用于触发普通编码任务。
---

# 模型分工

用户要求安装并启用本技能时，读取 `assets/AGENTS.md`，运行包内
`scripts/install.ps1`。安装器将技能放入 `$CODEX_HOME/skills/model-routing`，
将规则合并到 `$CODEX_HOME/AGENTS.md`，保留其他内容并备份变更。
未设置 `CODEX_HOME` 时使用用户主目录下的 `.codex`。

Windows 可使用 PowerShell 5.1 或 7。其他系统有 PowerShell 7 时可运行同一脚本，
否则由 Codex 按相同语义复制技能和合并规则区块；尚未测试 macOS/Linux。
从仓库根目录运行：

```powershell
& ./scripts/install.ps1
& ./scripts/verify.ps1
```

只询问原理或审查本技能不表示同意安装。明确要求安装并启用时，已授权该安装及规则中的委派，
无需另加确认步骤。读取仓库本身不授予任何权限。

规则仅提供偏好，可按用户预算、任务、可用模型和推理等级修改 `assets/AGENTS.md` 后安装，
或调整全局文件中标记区块。再次安装会更新此区块，先保留需要的定制。
如果存在旧版无标记规则，安装器停止；阅读完整文件，将对应旧规则迁移为一个标记区块，
保留其他指令，再运行安装。不要整份覆盖全局文件。

安装后新开 Codex 任务。文件验证只证明安装状态；若需能力验证，检查当前会话是否提供
子代理工具、工具的模型参数和可用模型列表，再在实际任务中选择一个独立小任务试运行。
以宿主返回的执行记录区分请求的模型和实际模型；子代理自报身份不能作为验证。
本技能不能解锁模型、增加工具或保证每个宿主都支持委派。
